# Tier 2 Prep Notes

Findings surfaced during Tier 1 boundary review that need a decision when their owning system is specced at Tier 2. Capture-don't-chase: one line per finding, decision deferred.

---

## Next-session pickup (as of 2026-05-26)

**Open task: the `system/`-coherence decision.** Builders boundary review is **CLOSED** (2026-05-25, husk #5 across the foundation tier / #3 within `system/` — see Builders finding below). With Builders gone, `system/` stands at exactly two slots: `Logger` (clean keep) and `GarbageCollector` (keep-greenfield, rescoped). The live question carried since FailureHandler is now **the** decision on the table:

> **Does `system/` remain a Tier-1 system, or do its two survivors (`Logger`, `GarbageCollector`) relocate to `lib/` and `system/` dissolve as a system?**

- This is a **system-shape decision**: it would change the system count (27 → 26 if `system/` dissolves), touches FileTree-v2 + PROJECT-FRAME, and affects the platform-Worker grouping (`services`/`system`/`state`/`library`). NOT a quick call — own framing, own research, own diff-read.
- Open it **fresh**, not at the tail of a long session. The Builders commit is the clean pause before it.
- **Log-lifecycle input (added 2026-05-26):** the Logger + GarbageCollector log-lifecycle decision (see Log-lifecycle bullet below) gives the two survivors a *positive* shared domain — the strongest form of the case for **A**. **Weigh** it in the call; it does NOT foreclose A, and GC's broader retention scope (not logs-only) stays in play.
- Also parked for later reviews (not blockers): (a) **social-platform API coverage** — `social/` is annotated "direct platform API posting" but enumerates only Planner/Manager/Collator/Analytics; `integrations/` enumerates only `YouTubeAPI` — confirm where IG/FB/LinkedIn/X/TikTok/Pinterest post/schedule adapters live (integrations/ review). (b) **orchestration/QueueHandler** + **v1 Archiver orientation** — see bullets below.

---

- **orchestration/QueueHandler** — v1 design (per `DOCS - Preplanning/DOCS - SystemPlan/Modules/QUEUEHANDLER.md` + `DOCS - StructureDefined/PhasesPlan/Core Planning/02-SYSTEMS.md` Manager→Workers→Functions pattern) is a rich priority scheduler (5 levels, brand fairness, deadline weighting, token-budget scoring) + WorkerManager dispatch layer; **conflicts** with `PLATFORM-GAP-ANALYSIS.md` §7 line 614 SUPERSEDED verdict ("use native Cloudflare Queues"). Cloudflare Queues is generic transport, not a priority scheduler. Decision at orchestration/ Stage 2: drop scheduling intelligence (use Queues bare) vs keep (port the v1 design on top of Queues + D1). Do NOT ratify the SUPERSEDED verdict the way DatabaseHandler/PythonRunner were.

- **v1 Archiver spec orientation** (`DOCS - StructureDefined/PhasesPlan/Core Planning/01-ARCHITECTURE.md` §Archiver, lines 1230–1342) — substantive v1 design exists for an Archiver that did NOT carry into v2 FileTree. Its filesystem/single-process mechanics are dead on V8 isolates (same shape as PythonRunner/DatabaseHandler — do not port). But its two real responsibilities map cleanly to existing v2 owners: **Job A — content-piece archival → `library/Archivist`** (extends lifecycle-write role to include archival transitions); **Job B — operational data housekeeping** (retention rules: logs 30d, error logs 90d, token usage 7d, cost rollups 30d, etc.) **→ `system/GarbageCollector`** (Cron-triggered stale-row cleanup in D1). Read the v1 spec at Stage 2 for **retention-policy intent and event-driven trigger model**, not for mechanics. Nothing to add to FileTree — capability already split correctly across two existing subsystems.

- **`system/` Tier-1 coherence question** — Tier-1 boundary review has now reviewed all five original `system/` slots. **Three are v1 husks that do not survive as `system/` subsystems:** **FailureHandler** dropped 2026-05-24 (OQ-FH-1; v1 intelligence module decomposed wholesale into v2 `resilience/`); **Parsers** does not survive (2026-05-25; parse-half dead, validate-half → shared validator, broader validation → own coordination layer — see Parsers finding below); **Builders** does not survive (2026-05-25; both v1 definitions collapse, no v2 slot — see Builders finding below; `generateId` relocates to `lib/`). **The two survivors:** **Logger** (clean keep) and **GarbageCollector** (keep-greenfield, rescoped — Cron-triggered stale-row cleanup in D1, NOT v1's filesystem cleanup). **So `system/` now stands at two slots, both arguably plumbing — and the coherence question is no longer a "live possibility," it is THE decision on the table:** does `system/` remain a Tier-1 *system*, or do `Logger` + `GarbageCollector` relocate to `lib/` and `system/` dissolve (system count 27 → 26)? **System-shape decision — own framing/research/diff-read; do NOT spec `system/` before it is settled.** This is the most consequential output of the Tier-1 `system/` cluster review. (See Next-session pickup at top.)

- **Log-lifecycle decision (2026-05-26) — Logger + GarbageCollector shared domain.** *(Worked out by Natasha in the 2026-05-26 session discussion — outside Claude Code's context window but in-thread; captured per CLAUDE.md §11 as plain in-thread authorship, not imported recall.)* Decided:
  - **Logger persists logs to D1.**
  - **GarbageCollector archives logs from D1 → R2 (quarterly) and prunes.**
  - **Local archiver** = **separate user-side program (VS Code / Claude Code local tool), OUT of NCE-V2 scope** — same decoupled pull-shape as the website CLI-pull pattern: NCE-V2 surfaces state in R2, the user's local tool pulls, NCE-V2 never writes to local hardware.
  - **GC retention shape — OPEN:** "clear-everything-quarterly vs keep-recent-window." Not pinned; resolve when GC is specced.
  - **Reconcile — EXTENDS, does not replace, the recorded scope (flag):** the committed record has GC as *broad* operational-data housekeeping (logs / error-logs / token-usage / cost-rollups — see v1-Archiver bullet above) and Logger as a *general* structured-emit primitive. This log-lifecycle framing is logs-centric and adds two *new* behaviours — GC **archives to R2** (richer than "D1 stale-row cleanup") and Logger **persists to D1** (beyond "emit"). Logs are ONE category of GC's retention work; the log lifecycle is one instance. The coherence call must use GC's **broad** scope, not narrow it to "logs only."
  - **Parked technical verify (spec-time):** persisting logs to D1 has D1 write-volume implications (D1 has write caps; high-volume logging is often moved to Workers Logs / Logpush). Confirm logging volume fits D1, or revisit the sink, when Logger / GC are specced.
  - **Bearing on the coherence decision — INPUT, not the call.** Gives Logger + GC a *positive* shared domain (the log lifecycle) rather than merely "neither fits elsewhere" — the strongest form of the case for **A** (keep a small coherent `system/`). To be **weighed** in the afternoon coherence decision against the full picture (incl. GC's broader retention scope). **A is not decided; this tees up the call, it does not foreclose it.**

- **Parsers — design direction: component-tree atoms + shared schema contract** *(worked out by Natasha across the 2026-05-24/25 sessions; informed by the website renderer briefing of 2026-05-25, see `docs/WEBSITE-RENDERER-STATE-2026-05-25.md`; durably captured 2026-05-25)*

  *The dilemma.* Freeform AI output cannot be trusted to be structurally consistent — parsing it after the fact is inference and guesswork. A fully rigid pre-determined JSON structure leaves the AI no room to compose. Neither extreme is acceptable.

  *The resolution — constrained creativity over a fixed atom set.* The AI is given a fixed set of allowed structural "atoms" and composes freely within that vocabulary; output is born tagged. Structural typing moves to **generation time**; Parsers does not do freeform structural inference.

  *What the website briefing established.* The website renderer already defines this vocabulary concretely: a single component registry (17 registered components as of 2026-05-25 — Page, Section, Grid, Heading, Text, etc.), consumed as a recursive component tree where each node names a registered `component` and carries flat props. The atom set is therefore **not a greenfield invention** — it is effectively the website's component registry, and the master set is **global** (one shared vocabulary, a tree, not per-content-type sets). Note: 8 of 17 component schemas were mid prop-rename audit at the briefing date — the vocabulary is actively changing.

  *Effect on Parsers' role.* Parsers shrinks to a **deterministic check**: every node's `component` is a member of the registered set, and its props satisfy that component's schema. This is a hard fail at NCE-V2 generation time. It is the **only validation NCE-V2's generated output ever receives in production**: the website's validators (DEV-mode renderer check, build-time `validate-data.ts`) run at build-time over the website's own `src/data/` files and do not fire on R2-fetched content at production request-time. The website renders an unregistered component as nothing — a silent failure — so NCE-V2 must catch malformed output before JSON reaches R2. The Parsers gate is not redundant; it is the sole gate.

  *Schema ownership — Natasha's lean.* A **shared component-schema source**: one canonical set of schema definitions consumed by both NCE-V2 and the website, neither holding a private copy. This is the drift fix — same pattern as the colour-math shared-module decision. A **shared validator** is viable: NCE-V2 content generation is JS/TS on Cloudflare Workers and the website is JS/TS on Cloudflare, so the website's `schema-validator.ts` logic can run on both sides — no language/runtime barrier. (Validator portability is a design inference from shared runtime, not a briefing-stated fact.)

  *Open sub-questions — for the Parsers boundary review.* (1) Where the shared schemas physically live — extracting them from the website repo into a neutral home is a **cross-repo coordination task, actioned in the website project, not from NCE-V2**. (2) Change-governance between the two consumers. (3) Versioning, given the in-progress prop-rename audit. (4) Schema delivery to the shared validator — build-time disk read (website) vs bundled/fetched at Worker runtime (NCE-V2); a packaging detail, not a blocker. (5) Subsetting at use level — whether a given content-type uses the full atom palette or a subset — is a separate template-level decision, not within scope here.

- **Parsers — boundary review finding (2026-05-25): `system/Parsers` does not survive as conceived.** Reviewed against v2 design + the captured atom-list direction (see Parsers atom-list bullet above); v1 `ROLE-MATRIX.md` Parsers entry (`parseJSON`, `parseAIResponse`, `validateSchema`, `extractContent`) and v1 `system/Parsers` placeholder used as **orientation only, not as v2 commitments**. Three confirmed sub-findings:
  1. **Parse/extract half — dead, no v2 job.** v1 Parsers' core job was structural inference: take a freeform AI blob and extract structure (`parseJSON`, `parseAIResponse`, `extractContent` — 3 of its 4 ROLE-MATRIX functions). The captured atom-list direction kills this: generation produces an already-tagged component tree, so there is no freeform blob to infer structure from. Same husk shape as PythonRunner / DatabaseHandler / FailureHandler — a v1 role the v2 design eliminated.
  2. **Validate half — belongs to the shared validator, not a bespoke `system/Parsers`.** The surviving `validateSchema` need is served by the **shared component-schema validator** from the schema-ownership lean (see atom-list bullet above), NOT by a reimplementation in `system/`. v1 evidence reinforces this: validation was already duplicated in v1 (both Parsers and FileManager validated against schemas) — the exact drift a single shared validator eliminates. Reimplementing it in `system/Parsers` would re-create that drift.
  3. **Broader validation need — a coordinated multi-checker stage; deferred to its own review.** *(Recorded from Natasha's account; this development was worked out by Natasha and is captured here from that account, not co-derived in the current session window — provenance noted honestly per CLAUDE.md §11.)* The real surviving validation need is bigger than a thin wrapper: a **coordinated multi-checker validation stage** — spelling, grammar, brand/values, safeguarding, structural — with a **manager** coordinating which checks a given job requires, and failures routed to the failure handler. (v1's ContentManager coordination pattern is orientation for the *shape*, not a v2 commitment.) Where that manager lives, which systems own which individual checks, and the failure-handoff contract are **its own dedicated review** — explicitly deferred, explicitly NOT specced as part of Parsers, and explicitly not a `system/` slot by default.

  **Headline:** parse-half dead, validate-half → shared validator, broader validation → its own coordination-layer review. None of it survives as a `system/Parsers` subsystem. Parsers is the third `system/` slot not to survive cleanly (see `system/`-coherence note above).

- **Builders — boundary review finding (2026-05-25): `system/Builders` does not survive as conceived.** The deciding slot for the `system/`-coherence question. Reviewed fresh from v2 design; v1 `ROLE-MATRIX.md` Builders entry and v1 `system/Builders` placeholder used as **orientation only, not v2 commitments**. The review surfaced that **v1 had two contradictory definitions of "Builders," and both collapse — for different reasons** (which is what makes the verdict robust rather than pattern-matched):
  - **Source A — path/ID utility** (ROLE-MATRIX: `createOutputPath`, `scaffoldBrand`→folderStructure, `validatePath` [symlink-safe], `deletePath`, `generateId`). **Dead by substrate:** V8 isolates have no filesystem — no paths to create/validate/delete, no folders to scaffold (same substrate-kill as DatabaseHandler / the v1 Archiver). 4 of 5 functions dead outright. The exception, `generateId`, was **tested not swept**: ID generation is a real, runtime-agnostic need, but `crypto.randomUUID()` is native on Workers and a house ID format is a ~10-line helper → **`lib/` material, not a `system/` subsystem** (same relocating-not-vanishing honesty as GarbageCollector; same `lib/`-helper pattern as the `lib/d1/`, `lib/errors/` candidates from earlier husks).
  - **Source B — content orchestrator** (v1 placeholder: "high-level builders for documents, emails, and social content… coordinates TemplateEngine, renderers… calls TemplateEngine.run()"). **Dead by ownership:** in v2 every output type already owns its own builder — `documents/DocumentBuilder`, `email/EmailComposer`, `social/SocialMediaManager`, cross-system coordination is `orchestration/Orchestrator`. Nothing left for a generic builder to orchestrate. (This is exactly what the original B-review Boundary? marker suspected.)
  - **v2-native confirmation (the inverted check — what does v2 *need* to build, independent of v1?):** the v2 tree has 15+ builder/composer/generator/factory subsystems — `website/`{PageComposer, SectionBuilder, MetadataBuilder}, `email/EmailComposer`, `documents/`{DocumentBuilder, TableBuilder, ChartBuilder, DocumentVariantGenerator}, `document-templates/TemplatePreviewBuilder`, `marks/`{LogoComposer, IconGenerator, IconSetBuilder, …}, `content/VariantGenerator`, `template/`{TemplateFactory, OutlineGenerator, ThemeGenerator}, `services/PromptBuilder`, `resilience/RecoveryStateBuilder`, `lib/svg/SVGPathBuilder` — **every one owned by the system that produces the thing it builds.** v2's rule is "the system that owns the output owns its builder." `system/Builders` is the lone generic outlier contradicting that rule. So the verdict holds from both directions: v1's jobs are gone (substrate + ownership) AND v2's design has no slot for a central generic builder. The human owner confirmed (per CLAUDE.md §11 — the one input the tree can't supply) that there is no unrecorded building need in mind that the tree doesn't already home.

  **Headline:** **husk #5 across the foundation tier** (PythonRunner, DatabaseHandler, FailureHandler, Parsers, Builders — 2 in `services/`, 3 in `system/`); **#3 within `system/`**. `system/Builders` does not survive as a subsystem — both v1 definitions collapse (Source A substrate, Source B ownership), v2's distributed-builder pattern has no place for a generic builder, and `generateId` relocates to `lib/` as a need (not a vanishing). This closes the `system/` cluster review: see `system/`-coherence note above — the coherence decision is now live.

- **Parked threads surfaced while probing the Builders gate (2026-05-25)** — neither a Builders issue; recorded so they don't evaporate:
  1. **Templates/content storage — RESOLVED, no action.** "Where does new template/content content go?" is owned by `library/` (`library/Writer` for the lifecycle-write, `library/Librarian` for access). Not a building need, not homeless.
  2. **Social-platform API integration — PARKED as a coverage check for the `integrations/` review.** `social/` is annotated "direct platform API posting + analytics" but enumerates only `SocialMediaPlanner/Manager/Collator/Analytics` (coordination-shaped); `integrations/` enumerates only `YouTubeAPI`. Confirm where the per-platform post/schedule adapters (Instagram/Facebook/LinkedIn/X/TikTok/Pinterest) live — specced somewhere (likely `integrations/`) or assumed-but-missing. Not verified against the tree yet; not a blocker for the `system/` work.

- **Parked verify-items for the next PROJECT-FRAME open (recorded 2026-05-25)** — durable so they don't evaporate; both are record-consistency fixes surfaced by the husk-#5 recount + the GarbageCollector survivor-reclassification, NOT folded into the Builders commit (one is in a different file):
  1. **FailureHandler amendment miscount — CONFIRMED wrong, fix on next PROJECT-FRAME open.** `PROJECT-FRAME.md` (OQ-FH-1 amendment, "Pattern flagged in same commit" row) reads *"Third husk drop… (PythonRunner, DatabaseHandler, FailureHandler). Two of the three are in `system/`."* Verified against the file: only **one** of those three (FailureHandler) is in `system/`; PythonRunner + DatabaseHandler are both `services/`. Should read "one of the three in `system/`, two in `services/`." Correct when PROJECT-FRAME is next open for the coherence decision (don't rewrite history for it alone).
  2. **Parsers finding wording — RECONCILE post-GC-reclassification.** The Parsers finding headline (this file, above) says *"Parsers is the third `system/` slot not to survive cleanly."* With GarbageCollector now classified a **survivor** (keep-greenfield), the husks within `system/` are FailureHandler (1st), Parsers (2nd), Builders (3rd) — so "third" is wrong if it meant husk-count, though it was defensible under the looser "third slot that isn't a clean keep" reading (FailureHandler, rescoped-GC, Parsers). Reconcile to one consistent counting when convenient; low-stakes (no number propagates from it), but it shouldn't sit inconsistent.
